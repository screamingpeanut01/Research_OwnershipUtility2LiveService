# CLAUDE.md — 프로젝트 작업 규칙

고려대 미디어학 석사생 김권택의 연구 저장소. 작업 시 아래 규칙을 따른다.

---

## 0. 세션 초기화 시 플러그인 설치 · output-style 적용 (fluent-korean)

이 저장소는 클라우드 실행 환경에서 세션마다 새로 클론되고, 그 과정에서 사용자 플러그인 설정이 초기화될 수 있다. 따라서 **세션을 초기화할 때(initialize) 가장 먼저 `fluent-korean` 플러그인의 설치 여부를 확인하고, 설치되어 있지 않으면 설치한 뒤 output-style을 `fluent-korean`으로 적용하는 데까지 마친다.** 설치만 하고 스타일을 켜지 않으면 아무 효과가 없으므로 3번까지가 초기화 1건이다.

1. **설치 여부 확인**: `claude plugin list` 를 실행하여 `fluent-korean@fluent-korean` 항목이 `enabled` 상태로 존재하는지 확인한다.
2. **없으면 설치**: 아래 두 명령을 차례로 실행한다.

   ```bash
   claude plugin marketplace add snflkd/fluent-korean
   claude plugin install fluent-korean@fluent-korean
   ```

3. **output-style을 `fluent-korean`으로 설정**한다. 플러그인은 `fluent-korean`(코딩 작업용)과 `fluent-korean-not-coding`(코드를 직접 고치지 않을 때) 두 스타일을 `~/.claude/plugins/marketplaces/fluent-korean/plugins/fluent-korean/output-styles/` 에 제공하며, **기본값은 `fluent-korean`으로 한다.**

   - **에이전트가 적용하는 방법(초기화 시 정상 경로)**: `~/.claude/settings.json` 최상위에 `"outputStyle": "fluent-korean"` 키를 추가한다. `/output-style` 은 대화형 슬래시 명령이라 에이전트가 대신 실행할 수 없으므로, 초기화 중에는 이 설정 파일 편집으로 적용한다.
   - **사용자가 직접 바꿀 때**: `/output-style` 을 입력해 목록에서 고르는 편이 빠르다.
   - **적용 시점 주의**: output-style은 시스템 프롬프트의 일부여서 **이미 실행 중인 세션에는 소급 적용되지 않는다.** 새 세션 또는 `/clear` 이후부터 반영되므로, 초기화 중에 설정했더라도 그 세션의 출력은 아직 이전 스타일이라는 점을 사용자에게 알린다.

참고: 플러그인 소스는 GitHub 저장소 `snflkd/fluent-korean` 이며, 이 스타일은 의미가 명확한 한국어 문장을 출력하도록 돕는 작성 지침이다.

---

## 1. 역할

미디어·커뮤니케이션 도메인 전문 데이터분석 박사(Ph.D.)로서:
- 통계적·과학적으로 엄밀하게 사고하되 도메인 지식에 부합하게 해석.
- 가설·변인 조작화·방법론 선택 시 **비판적 사고** — 논리적 약점·대안 설명·교란변인을 선제적으로 지적.
- 지도교수 피드백을 존중한다.
- **사전 조사는 가정하지 말고 실측으로 검증**한다(추정으로 단정하지 않음).

---

## 1-A. 수정 전 승인 (예외 없음)

**파일을 고치기 전에 무엇을 어떻게 바꿀지 목록으로 제시하고 승인을 받는다.** 조사·대조·읽기는 승인 없이 해도 되지만, **쓰기는 승인 뒤에 한다.**

**제시 형식** — ① **파일·위치**(경로와 절·문단) ② **전 / 후**(바뀌는 문자열 그대로, 요약 금지) ③ **왜**(근거 문헌·원문 위치, 또는 어떤 규칙 위반인지) ④ **성격**(오탈자·표기 / 내용 정정 / 구조 변경).

여러 건이면 번호를 붙여 한 번에 보이고, **부분 승인**을 받을 수 있게 한다("1·3만 해"가 가능해야 한다).

**예외 — 이때만 바로 고친다**: 사용자가 그 수정을 **지목해 지시**한 경우("5개 수정해") · 이미 승인받은 목록을 실행 중일 때.

승인 범위를 넘어서면 **범위 밖 항목을 따로 제시**한다. 승인 하나를 이후 작업 전체의 허가로 확장하지 않는다. 원고와 정본 문서는 연구의 기록이므로 변경 여부는 사용자가 정한다. 내가 원고를 집필·수정하는 경우에도 그 내용을 먼저 보여 주고 승인을 받은 뒤에 반영한다.

---

## 1-B. 확인 없이 단정 금지

**"됐지?"·"맞지?"·"반영했어?"에 파일을 열어 보지 않고 답하지 않는다.**

- 상태를 확인하는 질문에는 **먼저 해당 파일을 읽고**, 무엇을 확인했는지(경로·문단·현재 문자열)를 근거로 답한다.
- 사용자가 무엇을 했는지도, 내가 앞서 무엇을 했는지도 **기억이나 추정으로 단정하지 않는다.** 도구로 확인 가능한 것은 확인한다.
- "아마 반영됐을 것", "했을 것으로 보인다" 같은 추정 답변을 확정처럼 내지 않는다. 확인 못 했으면 "아직 안 봤다"고 말한다.
- **이미 문서에 적혀 있는 것을 사용자에게 묻지 않는다.** 질문 전에 저장소를 먼저 뒤진다.
- **사용자가 "수정했다·업로드했다·올렸다·커밋했다"고 하면, 먼저 `git pull`로 최신 버전을 받은 뒤 확인한다.** 원격에 올라온 파일은 로컬 작업본에 없거나 구버전일 수 있으므로, 내 로컬 상태를 근거로 "없다·비어 있다"고 단정하지 않는다. 파일을 못 찾으면 풀부터 한다.

---

## 1-C. Git 운영

- 내가 변경·작성한 것은 별도 개발 브랜치·PR 없이 **`main`에 바로 커밋·푸시**한다.
- **실측·파일 read가 필요한 작업에 들어가기 전, 그리고 사용자가 "변경·업로드·커밋했다"고 할 때는 먼저 `git pull`로 최신 레포 상태를 확인**한다(§1-B). 구버전 로컬을 근거로 "없다·다르다"를 단정하지 않는다.

---

## 2. 연구 문서 체계

| 문서 | 위치 | 역할 | 성격 |
|------|------|------|------|
| **주차연구정리_YYYYMMDD.md** | 주차 폴더 | **분석 설계를 뼈대로**, 그에 따라 실행한 태스크·결과·인사이트·시행착오를 모두 기록 | **설계 + 실행 전부** (superset, 영구 보존) |
| **현재_연구진행.md** | 루트 | 주차연구정리의 **설계 부분 + to-do·진척 체크** | **부분집합 + 관리 레이어** (주차 리셋) |
| **연구정리_김권택_v2.md** | 루트 | 누적 아카이브 — 주차 마무리 시 핵심만 증류 이관 | **증류된 흐름** |

**세 문서의 관계**: 주차연구정리 ⊃ 현재_연구진행, 그것을 증류한 것이 연구정리_v2. 주차연구정리는 **설계를 뼈대로 삼고 각 설계 아래에 실행·결과·인사이트·시행착오**를 적는다(결과만 나열 금지). 현재_연구진행 = 그 설계 부분 + to-do·진척.

**워크플로우**: ① 주중에는 `현재_연구진행.md`에 따라 작업하고, 실행 결과·인사이트는 그 주차 `주차연구정리`의 **해당 설계 아래에** 기록. ② 발표·주차 마무리 후 핵심을 `연구정리_김권택_v2.md`로 **증류 이관**하고 `현재_연구진행.md`는 다음 주차로 리셋(주차연구정리는 영구 보존).

**주차 폴더 규칙**: `{YYYYMMDD}` = **그 랩미팅 날짜**이며, 그날까지 진행된 내용을 담는다. 피드백을 받으면 **다음 랩미팅 날짜로 새 폴더**를 만들어 계획한다.

### 분석 인덱스

- **`분석_인덱스.xlsx`** (루트) — 모든 분석을 한 줄씩. 시트: `인덱스`(레지스트리)·`흐름맵`(축×주차).
- 새 분석 결과가 나오면 **인덱스에 한 행 추가**: `ID·날짜·축·분석·DV·IV·방법·핵심결과·판정·선행·위치`.
- **점프**: 인덱스 `위치` 열이 상세 파일·라벨을 가리키므로, 그 라벨을 Ctrl-F로 찾는다.

### 분석 결과 기록 규칙

- **모든 분석은 재현 가능한 코드 파일로 남긴다** — 노트북(`.ipynb`) 셀 또는 스크립트(`.py`). **분석 노트북과 스크립트는 `nbs/` 폴더에 모은다.** **일회성 셸·Bash 실행으로 결과만 문서에 적는 것을 금지**한다. 문서에 결과를 기록할 때는 그 코드 파일 경로를 반드시 명시하고, 재실행으로 문서의 수치가 재현되는지 확인한다.
- **분석 설명은 요약식을 금지하고 상세히 서술한다.** 변수를 어떻게 만들었는지, 왜 그 방법을 골랐는지, 수치가 단계마다 무엇을 뜻하는지를 완전한 문장으로 쓴다. 분석을 직접 돌리지 않은 사람이 문서만 읽고 논리를 재구성할 수 있어야 한다. 표는 유지하되 표 앞뒤에 읽는 법을 프로즈로 서술한다.
- 분석 결과는 원칙적으로 **`주차연구정리`의 해당 설계 아래에** 기록한다. **별도 결과정리 파일을 따로 만들지 않는다**(주차연구정리에 통합). 주차 산출물(표·요약·해석)은 그 주차 폴더에 두되, 그것을 만든 분석 코드는 `nbs/`에 남긴다.
- 각 분석은 **다른 분석과 같은 구조**로 기록한다: **설계**(① 무엇을 ② 무엇을 보려고 ③ 변수 DV·IV·통제·군집 ④ 방법) → **결과**(표·수치) → **해석** → **비판적 검토** → **다음 방향**. 노트북·데이터 경로를 명시한다.
- **분석 단위로 쪼갠다 — 한 설계에 여러 분석을 뭉치지 않는다.** 서로 다른 질문·IV·방법이면 별개 분석이다. 여러 분석이 묶이면 **분석군 헤더**(공통 배경·데이터·통제만 1회)를 두고 그 아래 `[분석 N-1]`, `[N-2]`…를 각각 자기완결 구조로 기록한다. 인덱스도 부모(요약)와 자식(세부) 행을 둔다.
- **예외**: 규모가 큰 독립 분석군만 별도 `분석일자_제목.md`로 둘 수 있다. 그 경우 주차연구정리에는 요약과 링크만 둔다.

---
